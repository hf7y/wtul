<!-- Per-project scope marker -- this file was CREATED by
     `scheduler -i` from a dropped idea. It's just that idea,
     not a real scope doc yet -- flesh this out properly, or at
     least confirm it before the next unattended run treats it
     as gospel. -->

## Ideas (added via `scheduler -i`)

- **2026-07-22 14:58 (via `scheduler -i`):** scheduler flagged (2026-07-22): wtul has no .claude/FOCUS.md -- ROADMAP.md is the real scope file today, so `scheduler focus/status` come up empty. Reconciling FOCUS.md vs ROADMAP.md is realisateur's job (already flagged there, .idea dropped 2026-07-22) -- don't create a competing FOCUS.md from wtul's own automation until that decision is made.
